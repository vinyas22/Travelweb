import { HotelbookingDetails } from './hotelbooking-details';

describe('HotelbookingDetails', () => {
  it('should create an instance', () => {
    expect(new HotelbookingDetails()).toBeTruthy();
  });
});
