import { FlightbookingDetails } from './flightbooking-details';

describe('FlightbookingDetails', () => {
  it('should create an instance', () => {
    expect(new FlightbookingDetails()).toBeTruthy();
  });
});
